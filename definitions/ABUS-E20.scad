// Key length
kl=27;

// Combination cuts

// Shoulder
aspace = 4.62;

// Pin distance
pinspace = 3.1;

// Highest cut
hcut = ph - 2*tol - 4.66;

bump_addcutdepth = 0.2;
bump_addplatspace = 0.6;

 // Cut spacing
cutspace = 0.54;

// Cut angle
cutangle = 90;

// Plateau spacing of the cut
platspace = 0.25;
