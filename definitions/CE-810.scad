// Key length
kl=24.2;

// Combination cuts

// Anschlag
aspace = 4.35;

// Pin distance
pinspace = 3.8;

// Highest cut
hcut = ph - 2*tol - 3.83;

 // Cut spacing
cutspace = 0.5;

// Cut angle
cutangle = 90;

// Plateau spacing of the cut
platspace = 0.3;

bump_addplatspace = 0.0;
